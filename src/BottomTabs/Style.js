import {StyleSheet} from 'react-native';
import Theme from '../Utils/Theme';

export const styles = StyleSheet.create({
  View: {alignItems: 'center', justifyContent: 'center'},

  Image: {height: Theme.hp('3%'), width: Theme.wp('6%')},
});
